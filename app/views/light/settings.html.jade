extends ../layout

block head

block headerleft
	a(data-role="button", data-rel="back", data-theme="c", data-icon="arrow-l") Tillbaka

block headerright
	a(data-role="button", data-rel="back", data-theme="c", data-icon="check") Spara

block content
		div.ui-grid-a
			div.ui-block-a.narrow 
				h4 Grupp
			div.ui-block-b.wide 
				h4 Fördröjning
			- each light in lights
					div.ui-block-a.narrow 
						h4= light.name
					div.ui-block-b.wide 
						label(for=light.id + "_delay")
						input(type="range", id=light.id + "_delay", name=light.id + "_delay" min="0", max="30", step="5", value="0")

						
		